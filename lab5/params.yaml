prepare:
  test_size: 0.2
  random_state: 42

train:
  n_estimators: 100
  max_depth: 10
  random_state: 42

evaluate:
  min_r2: 0.65   ## if R2 score is below this, the pipeline fails and nothing deploys

